# Core API clients
openai>=1.12.0        # For GPT-4o-mini fallback
groq>=0.3.0           # For Groq API fallback

# HTTP and utilities
requests>=2.31.0      # For general HTTP calls if needed
python-dotenv>=1.0.0  # For loading .env keys safely
pyyaml>=6.0.1         # For reading config/policy files

# CLI + display
rich>=13.7.0          # For colored terminal output
prompt_toolkit>=3.0.43  # For interactive CLI UX (optional)
pyfiglet>=0.8.post1   # ASCII art header in the TUI
tiktoken>=0.7.0       # Token counting in the Elia chat widgets

# Elia TUI dependencies (imported when running apps/elia)
textual[syntax]==0.79.1; platform_system != "Windows"  # Needs tree-sitter-languages (non-Windows wheels ship)
textual==0.79.1; platform_system == "Windows"          # Skip syntax extra to avoid missing tree-sitter-languages on Windows
sqlmodel>=0.0.9
humanize>=4.6.0
click>=8.1.6
xdg-base-dirs>=6.0.1
aiosqlite>=0.20.0
click-default-group>=1.2.4
greenlet>=3.0.3
google-generativeai>=0.5.3
pyperclip>=1.8.2
litellm>=1.37.19
pydantic>=2.9.0
